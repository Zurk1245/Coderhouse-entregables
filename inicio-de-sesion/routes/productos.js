/*const express = require("express");
//const { isLogged } = require("../index")
const productosRouter = express.Router();

productosRouter.use(express.static("public"));

/*productosRouter.get("/", isLogged, (req, res) => {
    res.status(200);
});*/
